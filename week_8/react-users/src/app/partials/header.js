import React, { Component } from 'react';

const Header = (props) => {
    return (
        <nav>
            <h3 className="nav-wrapper">{props.title}</h3>
        
            </nav>
    )

}

export default Header; 